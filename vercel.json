{
  "buildCommand": "pip install -r requirements.txt && pip install gunicorn",
  "installCommand": "pip install -r requirements.txt && pip install flask-login",
  "framework": null,
  "regions": ["fra1"],
  "builds": [
    {
      "src": "app.py",
      "use": "python",
      "config": {"maxDuration": 120}
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
